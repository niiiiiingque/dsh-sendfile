# dsh-sendfile 0.1.4 升级说明

0.1.4（2026-09-06）按先生要求移除拖拽文件时的全屏遮罩页（「松开，添加到当前对话」虚线大框）：拖拽功能完整保留（拖入即加卡片、图片仍走 DSH 原生通道），拖拽过程中只显示系统自带的复制光标，不再弹出任何页面。无接口变更，22 项自动测试全过。

# dsh-sendfile 0.1.3 升级说明

0.1.3（2026-09-06，Mac mini 接手维护后首版）仅改输入区回形针图标：弃用 📎 emoji（斜的、仿真、偏大），改用 DSH 图标库同款竖直矢量图标（ic_ds_paperclip_outline_16，随主题色），按钮从 34px 缩为 28×28 无边框圆形，与 DSH 原生输入按钮规格一致。无接口与行为变更，宿主连接点同 0.1.2，22 项自动测试全过。

# dsh-sendfile 0.1.2 升级说明

本版把对话输入区收拢为一个回形针图标。点击它选择文档，或直接把 Word、Excel、PPTX、PDF、文本拖进对话。待发送文件仍以卡片显示在输入框内，发送后的文件和 AI 生成结果仍可预览、下载。

原来的“文件”按钮和齿轮按钮已删除。文件生成后由 AI 在回复中提供预览、下载链接；字符数和单文件大小上限位于 DSH「设置 → 常规 → 发文件插件」。

## 本机安装或升级

1. 退出 DSH Desktop，确认没有正在运行的任务。
2. 在本文件所在目录打开 macOS 终端。
3. 执行 `node scripts/install.mjs --apply`。
4. 安装器会先完整备份 DSH 的插件环境、会话、索引、附件和设置，校验备份后再安装；旧的 DragView 插件会保留但继续停用。
5. 重启 DSH Desktop。输入框应只看到一个回形针图标；打开「设置 → 常规」可见“发文件插件”。

安装器只适用于已核验的 DSH Desktop 2.0.4。若 DSH 已升级，先不要安装，需重新做兼容性核验。

如需回退，先退出 DSH，再执行安装器打印的备份目录对应命令：`node scripts/rollback.mjs <备份目录> --apply`。
